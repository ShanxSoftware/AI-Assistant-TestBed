# xAI Handler Test Bench

## Setup
```bash
pip install -r requirements.txt
# Symlink your library
ln -s /path/to/your-ai-library/src ai_library
# Or install it: pip install -e /path/to/your-ai-library